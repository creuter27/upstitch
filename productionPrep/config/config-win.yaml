# productionPrep — Windows-specific path settings
# These override the corresponding keys in config.yaml on Windows.

# Output directories for fetched documents (Z: = network share / mapped drive)
output_dir_invoice: Z:/import/upstitch/invoices
output_dir_delivery_note: Z:/import/upstitch/delivery-notes

shipping_labels:
  label_folder: "Z:/import/upstitch/shipping-labels/raw"

design_csv: Z:/import/designs/export-current-design.csv
